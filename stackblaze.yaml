services:
  - type: web
    name: mcp-server
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: python -m src.server
    autoDeploy: false
    envVars:
      - key: MCP_TRANSPORT
        value: sse
